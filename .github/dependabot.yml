# Dependabot keeps GitHub Actions pinned versions current.
# Python helpers are standard-library-only by design, so there is no pip ecosystem here.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
